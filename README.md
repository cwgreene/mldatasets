Set up to work with nanogpt.

Setup for workflow:

Link config directory as subdirectory in nanoGPT/config
Link data directory entries as subdirectories in nanoGPT/data

Probably not necessary, but eh.
